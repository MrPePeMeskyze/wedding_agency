<% provide(:title, 'Фотографии') %>
<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>
<h1>Фотографии</h1>
<table class="table"> 
	<thead> 
		<tr> 
			<th></th> 
			<th>Название</th> 
			<th>Фото</th>
			<th></th>
		</tr> 
	</thead> 
	<tbody> 
		<%= render @photos %>
	</tbody>
</table>

<% content_for :right do %>
	<%= link_to raw("<span class='glyphicon glyphicon-plus-sign'></span>Добавить"), {:controller => "photos", :action => "new"}, class: "btn btn-success" %>
<% end %>