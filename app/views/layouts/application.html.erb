<!DOCTYPE html>
<html lang="en">

	<%= render 'layouts/head' %>

	<%= render 'layouts/header' %>
	<body>
		<main>
			<div class="container">
				<%= yield %>
			</div>
		</main>

		<%= render 'layouts/footer' %>

		<%= render 'layouts/top_button' %>

	</body>
</html>
